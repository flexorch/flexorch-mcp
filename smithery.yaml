startCommand:
  type: http
  url: https://mcp.flexorch.com
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        description: "FlexOrch API key — get one at app.flexorch.com/settings"
    required:
      - apiKey
  exampleConfig:
    apiKey: ""
